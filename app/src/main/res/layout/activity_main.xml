<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"

    tools:context=".ui.MainActivity">

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="300dp"
        android:id="@+id/imageView"
        android:src="@drawable/instinct"
        android:scaleType="centerCrop" />

    <TextView
        android:text="Your Level"
        android:id="@+id/lvlId"
        android:layout_marginBottom="45dp"
        android:layout_above="@+id/currentXp"
        android:layout_centerHorizontal="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>


    <TextView
        android:text="Your Current Xp"
        android:id="@+id/currentXp"
        android:layout_marginBottom="45dp"
        android:layout_above="@+id/totalPokemon"
        android:layout_centerHorizontal="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <TextView
        android:text="You have @ out of @ Pokemon"
        android:id="@+id/totalPokemon"
        android:layout_above="@+id/pokemonListButton"
        android:layout_marginBottom="45dp"
        android:layout_centerHorizontal="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />


    <Button
        android:text="Check Your Pokemon"
        android:id="@+id/pokemonListButton"
        android:layout_marginBottom="5dp"
        android:layout_marginLeft="5dp"
        android:layout_marginRight="5dp"
        android:background="@color/accent"
        android:layout_alignParentBottom="true"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

</RelativeLayout>
